# Development-only provider credentials for this repository's live integration tests.
# Juex runtime config is .juex/juex.yaml; copy juex.yaml as that file instead.
PROVIDER_API_TYPE=
PROVIDER_API_BASE=
PROVIDER_API_KEY=
PROVIDER_API_MODEL=
