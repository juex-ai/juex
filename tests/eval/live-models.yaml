# Live-provider validation scope. Routine scripts rotate one ref from each
# section and record the last successful ref in .juex/live-model-rotation.json.
# Use the script-level --all-models flag when a larger change needs every ref in
# a section.
provider_smoke_models:
  - openai-codex:gpt-5.5
  - ark:doubao-seed-2.0-pro
  - ark-anthropic:minimax-m3
  - ref: ollama-local:qwen3.6
    scenario_expectations:
      schedule-routing: optional
  - clip-local:gemini-3.5-flash-low
  - clip-local-responses:gpt-5.3-codex-spark

compaction_eval_models:
  - openai-codex:gpt-5.5
  - ark:doubao-seed-2.0-pro
  - ark-anthropic:minimax-m3
  - ollama-local:qwen3.6
  - clip-local:gemini-3.5-flash-low
  - clip-local-responses:gpt-5.3-codex-spark
